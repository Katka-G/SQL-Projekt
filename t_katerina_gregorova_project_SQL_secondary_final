CREATE TABLE t_katerina_gregorova_project_SQL_secondary_final AS
	SELECT DISTINCT 
		e.country,
		e.year,
		e.gdp,
		e.gini,
		e.population
	FROM economies AS e
	WHERE 
		e.gdp IS NOT NULL
	ORDER BY
		e.country,
		e.year;
